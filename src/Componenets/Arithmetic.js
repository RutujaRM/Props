// Here we create a function and take parameter

const Add = (a) => {
  console.log(a);
};

const Sub = (b) => {
  console.log(b);
};

export { Add, Sub }; //here we use named module for exporting multiple values we ceate objects {} and passing fn name into it
